<!--components/w-tab-control.wxml-->
<view class="tab-control">
  <block  wx:for="{{titles}}" wx:key="id">
    <view class="item {{currentIndex==index?'active':''}}" data-index="{{index}}" bindtap="handleItem"><text>{{item}}</text></view>
  </block>
</view>
